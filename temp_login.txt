  // Vista de login
  if (!loggedIn) {
    return (
      <div className="min-h-screen bg-gradient-to-b from-[#3b0d0d] to-[#111827]">
        <div className="container mx-auto px-4 py-4">
          <div className="relative w-full max-w-xs mx-auto">
            <img
              src="/OrdLogo.png"
              alt="Logo"
              className="w-24 mx-auto mb-2"
            />
            <h1 className="text-lg font-bold text-white text-center">
              Ordoñez Group Inc.
            </h1>
            <p className="text-sm text-white/80 text-center mb-4">
              Cattle &amp; Butcher Management System
            </p>

            <form
              onSubmit={handleLogin}
              className="bg-black/20 backdrop-blur-sm border border-white/10 rounded-lg p-4"
            >
              <h2 className="text-base font-semibold text-white text-center mb-3">
                {t.loginTitle}
              </h2>
              <input
                name="user"
                placeholder={t.username}
                className="w-full mb-2 px-3 py-2 rounded bg-white/90 text-black text-sm"
                autoComplete="username"
              />
              <input
                name="pass"
                type="password"
                placeholder={t.password}
                className="w-full mb-3 px-3 py-2 rounded bg-white/90 text-black text-sm"
                autoComplete="current-password"
              />
              <button
                type="submit"
                className="w-full bg-red-700 hover:bg-red-800 text-white rounded px-4 py-2 text-sm font-semibold"
              >
                {t.login}
              </button>
            </form>

            <div className="absolute top-0 right-0">
              <select
                value={language}
                onChange={(e) => setLanguage(e.target.value)}
                className="bg-gray-800 text-white border border-gray-500 rounded px-2 py-1 text-sm"
              >
                <option value="en">🇺🇸 EN</option>
                <option value="es">🇪🇸 ES</option>
              </select>
            </div>
          </div>
        </div>
        <ToastContainer position="top-center" />
      </div>
    );